# prereq - install the CLI tools for Xcode
# includes compilers and etc.
sudo xcode-select --install

# install brew - the missing pkg mngr of macOS
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install.sh)"

# setup the PATH

# re-source/start the bash/zsh
